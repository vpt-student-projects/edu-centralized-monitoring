<Window x:Class="TechInventory.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему"
        Height="400" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление IT-инфраструктурой" FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"
                   Grid.Row="0"/>

        <TextBox Grid.Row="1" x:Name="LoginTextBox" 
                 Margin="0,5" Padding="3"/>

        <PasswordBox Grid.Row="2" x:Name="PasswordBox" 
                     Margin="0,5" Padding="3"/>

        <Button Grid.Row="3" Content="Войти" Click="LoginButton_Click"
                Height="30" Margin="0,15,0,0"/>

        <Button Grid.Row="4" Content="Регистрация" Click="RegisterButton_Click"
                Height="30" Margin="0,5,0,0"/>

        <TextBlock Grid.Row="5" x:Name="ErrorTextBlock" Foreground="Red" 
                   TextWrapping="Wrap" Margin="0,10,0,0"/>
    </Grid>
</Window>