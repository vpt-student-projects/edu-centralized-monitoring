<Window x:Class="TechInventory.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TechInventory.ViewModels"
        Title="TechInventory — Главная"
        Height="680" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="12,0,10,0"/>
            <Setter Property="BorderBrush" Value="#D0D5DD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.06" Color="#000"/>
                            </Border.Effect>
                            <Grid>
                                <TextBlock x:Name="Placeholder" Text="🔍  Поиск устройства…"
                                           VerticalAlignment="Center" Margin="12,0,0,0"
                                           Foreground="#9CA3AF" FontStyle="Italic" FontSize="14"
                                           Visibility="Collapsed" IsHitTestVisible="False"/>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0078D4"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#9ABFDB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#E8F0FE"/>
                                <Setter Property="Foreground" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#D0E4F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль ToggleButton для меню – похож на боковую кнопку -->
        <Style x:Key="MenuToggleStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#E8F0FE"/>
                                <Setter Property="Foreground" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#E8F0FE"/>
                                <Setter Property="Foreground" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#0078D4" Padding="16,18">
                    <StackPanel>
                        <TextBlock Text="🖥  TechInventory" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Управление IT-инфраструктурой" FontSize="11" Foreground="#A8D4F5" Margin="0,3,0,0"/>
                    </StackPanel>
                </Border>

                <TreeView Grid.Row="1" ItemsSource="{Binding Buildings}"
                          BorderThickness="0" Background="Transparent"
                          Margin="8,8,8,0"
                          SelectedItemChanged="TreeView_SelectedItemChanged">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type vm:BuildingNodeViewModel}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="🏢 " FontSize="13"/>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" Foreground="#111827"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vm:FloorNodeViewModel}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="📋 " FontSize="13"/>
                                <TextBlock Text="{Binding Name}" FontSize="13" Foreground="#374151"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        <DataTemplate DataType="{x:Type vm:RoomNodeViewModel}">
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="📂 " FontSize="13"/>
                                <TextBlock Text="{Binding Name}" FontSize="13" Foreground="#4B5563"/>
                            </StackPanel>
                        </DataTemplate>
                    </TreeView.Resources>
                </TreeView>

                <StackPanel Grid.Row="2" Margin="8,0,8,14">
                    <Separator Margin="0,6,0,8" Background="#E5E7EB"/>
                    <ToggleButton x:Name="MenuToggle" Content="☰  Меню" 
                                  Style="{StaticResource MenuToggleStyle}" 
                                  IsChecked="False" Margin="0,2"/>
                    <StackPanel x:Name="ButtonPanel"
                                Visibility="{Binding IsChecked, ElementName=MenuToggle, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Button Content="📋  Лента заявок" x:Name="TicketsBtn" Click="TicketsButton_Click" Style="{StaticResource SidebarButton}" Margin="0,2"/>
                        <Button Content="➕  Создать заявку" x:Name="CreateTicketFromMainButton" Click="CreateTicketFromMain_Click" Style="{StaticResource SidebarButton}" Margin="0,2"/>
                        <Button Content="📊  Отчёты" Click="ReportsButton_Click" Style="{StaticResource SidebarButton}" Margin="0,2"/>
                        <Button Content="📁  Кабинеты" x:Name="RoomsButton" Click="RoomsButton_Click" Style="{StaticResource SidebarButton}" Margin="0,2"/>
                        <Button Content="📚  Справочники" Click="DictionaryButton_Click" Style="{StaticResource SidebarButton}" Margin="0,2"/>
                        <Button Content="👤  Пользователи" x:Name="UsersButton" Click="UsersButton_Click" Style="{StaticResource SidebarButton}" Margin="0,2"/>
                        <Button Content="🚪  Выйти" Click="LogoutButton_Click" Style="{StaticResource SidebarButton}" Margin="0,2"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="20,18,20,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding SelectedRoom.RoomName}"
                       FontSize="22" FontWeight="SemiBold" Foreground="#111827" Margin="0,0,0,14"/>

            <TextBox Grid.Row="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource SearchTextBoxStyle}"
                     Width="320" HorizontalAlignment="Left" Margin="0,0,0,16"
                     Visibility="{Binding IsRoomSelected, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Devices}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" ItemWidth="176" ItemHeight="176"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:DeviceTileViewModel}">
                            <Border Margin="6" Padding="14" CornerRadius="14"
                                    Background="#FFFFFF" Cursor="Hand"
                                    MouseLeftButtonDown="DeviceTile_Click">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.09" Color="#000000"/>
                                </Border.Effect>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasOpenTickets}" Value="True">
                                                <Setter Property="BorderBrush" Value="#D13438"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F5F8FF"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Icon}" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"
                                               HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"
                                               MaxWidth="148" ToolTip="{Binding Name}" Foreground="#111827"/>
                                    <TextBlock Text="{Binding StatusName}" FontSize="12" HorizontalAlignment="Center"
                                               Margin="0,5,0,2"
                                               Foreground="{Binding StatusName, Converter={StaticResource StatusToColorConverter}}"/>
                                    <TextBlock Text="⚠ Есть заявки" Foreground="#D13438" FontWeight="SemiBold" FontSize="11"
                                               Visibility="{Binding HasOpenTickets, Converter={StaticResource BoolToVisibilityConverter}}"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding AssignedTo}" FontSize="11" Foreground="#9CA3AF"
                                               HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="148"
                                               Visibility="{Binding AssignedTo, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>